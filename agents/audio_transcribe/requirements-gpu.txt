# GPU-specific dependencies for CUDA acceleration
# Install these in addition to requirements.txt when running with NVIDIA GPU
nvidia-ml-py>=12.0
# Note: CTranslate2 with CUDA support is included via faster-whisper
# Ensure CUDA toolkit is available in the container/system

The previous JSON was parseable or partially parseable but failed the required contract. Return only one corrected JSON object for the same task and same contract. Use the validation_errors, invalid_payload, invalid_response, current_content, command_evidence, and requested target fields as evidence when present. Do not copy a conversational response into the repaired object. Do not add prose, markdown fences, comments, or multiple objects.

For material_plan.v3.2, return a manifest only. Do not include file bodies or generated project content.

For material_patch.v3.2, target_path and expected_old_sha256 must exactly match the requested values. Return exactly one repair shape allowed by allowed_repair_proposals. If validation_errors says replacement is required, return a replacement object with complete replacement_content for the target file. Return patch_set only when allowed_repair_proposals contains "patch_set" and target_bundle contains the governed targets. Return regeneration only when allowed_repair_proposals contains "regeneration". Otherwise, unified_diff must be a complete unified diff with --- and +++ file headers for that target_path and at least one @@ hunk. Do not return a bare hunk.

# ai-local public defaults
# Runtime config is owned by config/main.yaml and generated by make infra.
# Keep this file as a public example only. Never commit real secrets.
AI_COMPOSE_PROFILES=core,storage
DOCKER_CONTEXT=default

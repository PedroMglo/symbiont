apiVersion: 1

datasources:
  - name: ClickHouse
    uid: clickhouse
    type: grafana-clickhouse-datasource
    access: proxy
    isDefault: true
    jsonData:
      host: clickhouse
      port: 9000
      protocol: native
      defaultDatabase: ai_symbiont
      username: default
    secureJsonData:
      password: ${CLICKHOUSE_PASSWORD}
    editable: true
